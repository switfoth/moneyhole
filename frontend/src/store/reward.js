const ADD_REWARD = '/addReward';
const UPDATE_REWARD = '/updateReward';
const DELETE_REWARD = '/deleteReward';

export default function rewardReducer(state = {}, action) {
    switch (action.type) {
        default:
            return state;
    }
}
