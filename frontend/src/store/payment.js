const ADD_PAYMENT = '/addPayment';
const UPDATE_PAYMENT = '/updatePayment';
const DELETE_PAYMENT = '/deletePayment';

export default function paymentReducer(state = {}, action) {
    switch (action.type) {
        default:
            return state;
    }
}
