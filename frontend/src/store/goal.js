const ADD_GOAL = '/addGoal';
const UPDATE_GOAL = '/updateGoal';
const DELETE_GOAL = '/deleteGoal';

export default function goalReducer(state = {}, action) {
    switch (action.type) {
        default:
            return state;
    }
}
