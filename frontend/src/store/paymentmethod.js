const ADD_PAYMENTMETHOD = '/addPaymentMethod';
const UPDATE_PAYMENTMETHOD = '/updatePaymentMethod';
const DELETE_PAYMENTMETHOD = '/deletePaymentMethod';

export default function paymentReducer(state = {}, action) {
    switch (action.type) {
        default:
            return state;
    }
}
